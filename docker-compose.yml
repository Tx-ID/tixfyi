version: '3'

services:
  tixfyi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tixfyi
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
